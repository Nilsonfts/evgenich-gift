# 🔍 Проверка Auto-Sync в Google Sheets на Railway

## Проблема
При регистрации пользователя через `/start` команду в боте — пользователь должен **автоматически** появиться в Google Sheet `Выгрузка Пользователей` за **несколько секунд**.

## Решение
Добавлено **подробное логирование** в функциях `add_new_user()` и `_add_user_to_sheets_in_background()`.

## Как Проверить на Railway

### Шаг 1: Проверь логи Railway
```bash
railway logs --follow
```

Ищи сообщения типа:
```
📝 Подготовка данных пользователя {user_id} для Google Sheets...
📝 GOOGLE_SHEETS_ENABLED=True, GOOGLE_SHEET_KEY=True, GOOGLE_CREDENTIALS_JSON=True
✅ Запускаю фоновую задачу добавления пользователя {user_id} в Google Sheets...
G-Sheets (фон) | Попытка добавления пользователя. GOOGLE_SHEETS_ENABLED=True
G-Sheets (фон) | Подключаюсь к worksheet для добавления пользователя {user_id}...
✅ Пользователь с ID {user_id} успешно добавлен в таблицу.
```

### Шаг 2: Отправь тестовую команду боту
В Telegram:
1. Открой бота
2. Пошли команду `/start`
3. Бот должен ответить с приветствием

### Шаг 3: Проверь Google Sheet
Перейди по ссылке: https://docs.google.com/spreadsheets/d/1bp7NwfWe1MCb7S6wkaQtaxJEIDaFcFPwv9V_kzlchXs/edit?gid=0

**Что искать:**
- Нова строка должна появиться в таблице за 1-3 секунды
- Если логи показывают ошибки — скопируй их ниже для диагностики

### Шаг 4: Если что-то не работает
Проверь вывод логов на наличие ошибок:

#### Возможная ошибка 1: Google Sheets отключен
```
⚠️ Google Sheets отключен для пользователя {user_id}!
```
**Проверь:** Установлены ли env переменные на Railway?
```
railway variables
```
Должны быть:
```
GOOGLE_SHEET_KEY=1bp7NwfWe1MCb7S6wkaQtaxJEIDaFcFPwv9V_kzlchXs
GOOGLE_CREDENTIALS_JSON={...service account...}
```

#### Возможная ошибка 2: Невалидные credentials
```
G-Sheets | Не удалось парсить GOOGLE_CREDENTIALS_JSON
```
**Проверь:** JSON credentials должны быть корректным JSON-ом (не double-encoded).

#### Возможная ошибка 3: Лист не найден
```
G-Sheets | Лист 'Выгрузка Пользователей' не найден
```
**Проверь:** Имя листа в Google Sheet должно быть точно **"Выгрузка Пользователей"** (кириллица).

## Структура Строки в Таблице

Когда пользователь регистрируется, добавляется строка:
```
[
  '2026-01-04 19:30:00',  # Дата регистрации
  123456789,              # User ID (Telegram ID)
  'John',                 # First name
  'john_username',        # Username
  '',                     # Номер телефона (пусто при регистрации)
  '',                     # Реальное имя (пусто при регистрации)
  '',                     # Дата рождения (пусто при регистрации)
  'Зарегистрирован',      # Статус
  'direct',               # Источник (откуда пришел пользователь)
  '',                     # Реферер ID (если был)
  ''                      # Дата погашения (пусто при регистрации)
]
```

## После Регистрации - Синхронизация Данных

Когда пользователь вводит данные в боте, они также синхронизируются:

1. **Поделился контактом** → обновляется номер телефона (колонка E)
2. **Ввел имя** → обновляется реальное имя (колонка F)
3. **Ввел дату рождения** → обновляется дата рождения (колонка G)
4. **Получил купон** → статус меняется на "Купон выдан" (колонка H)
5. **Погасил купон** → статус меняется на "Купон погашен" + дата (колонка H + I)

## Если Все Работает ✅
1. Появляются логи о добавлении в Sheets
2. Новые строки видны в Google Sheet за несколько секунд
3. Обновления данных синхронизируются в реальном времени

## Контрольный Список

- [ ] Проверил логи Railway (`railway logs --follow`)
- [ ] Отправил `/start` боту
- [ ] Видна новая строка в Google Sheet за 1-3 сек
- [ ] Нет ошибок в логах
- [ ] Все колонки заполнены корректно

**Готово!** Auto-sync в Google Sheets работает! 🎉
