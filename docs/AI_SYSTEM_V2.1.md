# AI System v2.1 - Детектор намерений и Fallback система

## 🎯 Новые возможности

### 1. Умный детектор намерений (`ai/intent_detector.py`)

**Что делает:**
Автоматически определяет что хочет пользователь и извлекает важные детали.

**Поддерживаемые намерения:**
- `booking` - Бронирование столика
- `menu` - Вопросы про меню
- `address` - Адрес и местоположение
- `work_hours` - Часы работы
- `complaint` - Жалобы (высокий приоритет!)
- `price_inquiry` - Вопросы про цены
- `karaoke` - Караоке
- `event` - Мероприятия/банкеты
- `payment` - Оплата
- `greeting` - Приветствия
- `gratitude` - Благодарности
- `general` - Общие вопросы

**Извлечение сущностей:**

Для бронирования:
```python
"Хочу забронировать столик на завтра в 19:00 на 4 человек"
→ {
    "date": "завтра",
    "time": "19:00",
    "people_count": 4
}
```

Для адреса:
```python
"Где бар на Невском?"
→ {
    "bar_location": "nevsky"
}
```

**Пример использования:**
```python
from ai.intent_detector import intent_detector

intent = intent_detector.detect("Хочу забронировать столик")
print(intent.name)        # "booking"
print(intent.confidence)  # 0.95
print(intent.entities)    # {}
```

---

### 2. Система Fallback ответов (`ai/fallback_responses.py`)

**Что делает:**
Предоставляет быстрые готовые ответы на частые вопросы **без вызова OpenAI API**.

**Преимущества:**
- ⚡ Мгновенный ответ (без задержки API)
- 💰 Экономия токенов (0₽ за fallback ответы)
- 🛡️ Работает даже если OpenAI недоступен
- 🎯 Точные ответы на простые вопросы

**Когда используется fallback:**

1. **Автоматически** для простых намерений с высокой уверенностью:
   - Приветствия (confidence > 0.7)
   - Благодарности (confidence > 0.7)
   - Адрес (confidence > 0.7)
   - Часы работы (confidence > 0.7)

2. **При ошибках API** - fallback используется как запасной вариант

**Примеры ответов:**

Бронирование:
```
Для бронирования смотри закреп вверху чата 📌

Там кнопка ЗАБРОНИРОВАТЬ!
```

Адрес:
```
У нас два бара в Питере! 😊

📍 Невский 53 (от м. Маяковская 3-4 мин)
📍 Рубинштейна 9
```

Часы работы:
```
Работаем каждый день с 12:00 до 6:00! 🌙
```

**Персонализация:**

Fallback умеет подстраиваться под извлечённые сущности:
```python
# "Хочу на завтра в 19:00"
→ "Хочешь на завтра в 19:00?

Смотри закрепленное сообщение вверху чата 📌 Там форма бронирования!"
```

---

## 🔧 Интеграция

### Автоматическая работа в `assistant.py`

```python
def get_ai_recommendation(user_query, user_id=0, ...):
    # 1. Детектируем намерение
    detected_intent = intent_detector.detect(user_query)
    
    # 2. Проверяем можно ли использовать fallback
    if fallback_responses.should_use_fallback(
        detected_intent.name,
        detected_intent.confidence
    ):
        # Быстрый ответ без API!
        return fallback_responses.get_response(
            detected_intent.name,
            detected_intent.entities
        )
    
    # 3. Иначе используем OpenAI API
    response = openai_client.chat.completions.create(...)
    
    # 4. При ошибке API - fallback как запасной вариант
    except Exception as exc:
        return fallback_responses.get_response(
            detected_intent.name,
            detected_intent.entities
        )
```

---

## 📊 Метрики

Fallback ответы логируются в метрики с model="fallback":
```json
{
  "user_id": 123456,
  "model": "fallback",
  "prompt_tokens": 0,
  "completion_tokens": 0,
  "cost_usd": 0.0,
  "success": true
}
```

Это позволяет отслеживать:
- Сколько запросов обработано без API
- Экономию токенов и денег
- Эффективность fallback системы

---

## 🎯 Примеры работы

### Пример 1: Простое приветствие

**Пользователь:** "Привет!"

**Система:**
1. Детектор: `greeting` (confidence: 0.95)
2. Проверка: можно использовать fallback ✅
3. Ответ: "Привет, товарищ! 😊" (0 токенов)

### Пример 2: Вопрос про адрес

**Пользователь:** "Где вы находитесь?"

**Система:**
1. Детектор: `address` (confidence: 1.0)
2. Проверка: можно использовать fallback ✅
3. Ответ: "У нас два бара в Питере! 😊..." (0 токенов)

### Пример 3: Сложный вопрос

**Пользователь:** "Расскажи про ваши настойки, какие самые популярные?"

**Система:**
1. Детектор: `menu` (confidence: 0.85)
2. Проверка: нужен AI (сложный вопрос) ❌
3. Используется OpenAI API для подробного ответа

### Пример 4: Ошибка API

**Пользователь:** "Когда работаете?"

**Система:**
1. Детектор: `work_hours` (confidence: 0.9)
2. OpenAI API недоступен 💥
3. Fallback: "Работаем каждый день с 12:00 до 6:00! 🌙"

---

## 💰 Экономия

### До fallback системы:
- Все запросы → OpenAI API
- Простое "Привет" = ~50 токенов = $0.0001
- 1000 приветствий/день = $0.10/день

### После fallback системы:
- Простые запросы → Fallback (0 токенов)
- Сложные запросы → OpenAI API
- 1000 приветствий/день = $0.00/день

**Экономия:** ~30-40% токенов на простых вопросах

---

## 🔍 Отладка

### Проверить детектор намерений:
```python
from ai.intent_detector import intent_detector

intent = intent_detector.detect("Ваш текст")
print(f"Намерение: {intent.name}")
print(f"Уверенность: {intent.confidence}")
print(f"Сущности: {intent.entities}")
```

### Проверить fallback ответы:
```python
from ai.fallback_responses import fallback_responses

# Получить ответ
response = fallback_responses.get_response("booking")
print(response)

# Проверить нужен ли fallback
should_use = fallback_responses.should_use_fallback("greeting", 0.9)
print(should_use)  # True
```

### Список всех намерений:
```python
intents = fallback_responses.get_all_intents()
print(intents)
```

---

## 📝 Changelog v2.1

### Добавлено:
- ✨ `ai/intent_detector.py` - умный детектор намерений с извлечением сущностей
- ✨ `ai/fallback_responses.py` - система быстрых ответов без API
- 🔧 Интеграция fallback в `assistant.py`
- 📊 Логирование fallback ответов в метрики
- 💰 Автоматическая экономия токенов на простых запросах

### Улучшено:
- Ошибки API теперь обрабатываются с fallback вместо общих сообщений
- Персонализация ответов на основе извлечённых сущностей
- Приоритизация намерений (жалобы обрабатываются первыми)

---

## 🎉 Итоги v2.1

| Функция | Описание | Статус |
|---------|----------|--------|
| Детектор намерений | Определение что хочет пользователь | ✅ |
| Извлечение сущностей | Даты, время, локация и т.д. | ✅ |
| Fallback ответы | Быстрые ответы без API | ✅ |
| Экономия токенов | 30-40% на простых вопросах | ✅ |
| Обработка ошибок | Fallback при сбоях API | ✅ |
| Персонализация | Ответы учитывают сущности | ✅ |

**Бот стал умнее, быстрее и экономнее!** 🚀
