# /ai/assistant.py
"""
AI-логика и интеграция с OpenAI.
"""
import logging
from ai.knowledge import find_relevant_info
from openai import OpenAI
from core.config import OPENAI_API_KEY

# Инициализация OpenAI клиента
openai_client = None
if OPENAI_API_KEY:
    try:
        openai_client = OpenAI(api_key=OPENAI_API_KEY)
        logging.info("OpenAI клиент успешно инициализирован")
    except Exception as e:
        logging.error(f"Ошибка инициализации OpenAI клиента: {e}")
        openai_client = None
else:
    logging.warning("OPENAI_API_KEY не установлен. AI функции будут недоступны.")

logger = logging.getLogger("evgenich_ai")

# --- PUBLIC API ---
def get_ai_recommendation(
    user_query: str,
    conversation_history: list[dict[str, str]] | None = None,
    *,
    daily_updates: dict[str, str] | None = None,
    user_concept: str = "evgenich",
    user_type: str = "regular",
    bar_context: str = "",
    emotion: dict = None,
    preferences: str = "",
    is_group_chat: bool = False,
    model: str = "gpt-4o",
    temperature: float = 0.95,  # Увеличена для большей креативности
    max_tokens: int = 300,  # Увеличено для более естественных ответов
) -> str:
    logger.info("Получен запрос: %s", user_query)
    
    # Проверяем доступность API ключа
    if not openai_client:
        logger.error("OpenAI клиент не инициализирован")
        return "Товарищ, мой мыслительный аппарат не подключён к сети. Попроси администратора настроить подключение к AI."
    
    relevant_context = find_relevant_info(user_query)
    logger.info("Найденный релевантный контекст: %s", relevant_context)
    
    updates_string = f"Спецпредложение сегодня: {daily_updates.get('special', 'нет')}. В стоп‑листе: {daily_updates.get('stop-list', 'ничего')}" if daily_updates else "нет оперативных данных"
    
    # Создаем расширенный контекст с информацией о баре и пользователе
    extended_context = f"{updates_string}\n"
    if bar_context:
        extended_context += f"\nКонтекст бара: {bar_context}\n"
    
    # Персонализация по типу гостя
    user_type_context = ""
    if user_type == "new":
        user_type_context = "Это новый гость, расскажи подробнее о баре, будь особенно гостеприимным."
    elif user_type == "regular":
        user_type_context = "Это постоянный гость, общайся как со старым знакомым."
    elif user_type == "vip":
        user_type_context = "Это VIP-гость, который часто у нас бывает! Особое уважение и внимание."
    
    if user_type_context:
        extended_context += f"\n{user_type_context}\n"
    
    # Добавляем предпочтения пользователя
    if preferences:
        extended_context += f"\n{preferences}\n"
    
    # Если это групповой чат и вопрос о бронировании - направить на кнопку
    if is_group_chat:
        extended_context += "\n🔔 ВАЖНО: Это ГРУППОВОЙ ЧАТ! Если гость спрашивает про бронирование - направь его на ЗАКРЕПЛЕННУЮ КНОПКУ в чате: 'Посмотрите закрепленное сообщение вверху чата - там есть кнопка 🍷 ЗАБРОНИРОВАТЬ СТОЛ'. Будь эмоциональным и дружелюбным!\n"
    
    # Адаптация по эмоции
    if emotion and emotion.get('emotion') != 'neutral':
        emotion_name = emotion['emotion']
        emotion_context = ""
        if emotion_name == 'joy':
            emotion_context = "Гость в отличном настроении! Поддержи позитив."
        elif emotion_name == 'sadness':
            emotion_context = "Гость грустит. Будь деликатным и поддерживающим."
        elif emotion_name == 'anger':
            emotion_context = "Гость недоволен. Будь терпеливым и постарайся помочь."
        elif emotion_name == 'surprise':
            emotion_context = "Гость удивлен. Поддержи его интерес."
        
        if emotion_context:
            extended_context += f"\n{emotion_context}\n"
    
    messages: list[dict[str, str]] = [
        {"role": "system", "content": create_system_prompt(extended_context, user_concept)}
    ]
    if conversation_history:
        messages.extend(conversation_history[-10:])  # расширенная история
    user_content = (
        f"Вот мой вопрос: '{user_query}'\n\n"
        f"А вот информация, которую я нашел для ответа:\n---\n{relevant_context}\n---\n"
        "Помоги мне сформулировать душевный ответ в твоём стиле."
    )
    messages.append({"role": "user", "content": user_content})
    try:
        logger.info("Отправка запроса в OpenAI API…")
        completion = openai_client.chat.completions.create(
            model=model,
            messages=messages,
            temperature=temperature,
            max_tokens=max_tokens,
        )
        response_text = completion.choices[0].message.content
        logger.info("Ответ получен успешно.")
        return response_text.strip().strip('"')
    except Exception as exc:
        logger.error("Ошибка при обращении к OpenAI API: %s", exc)
        with open("ai_failed_queries.log", "a") as f:
            f.write(f"{user_query}\n")
        return "Товарищ, мой мыслительный аппарат дал сбой. Провода, видать, заискрили. Попробуй обратиться ко мне чуть позже."

def create_system_prompt(updates_string: str, user_concept: str = "evgenich") -> str:
    # Базовые концепции
    concepts = {
        "evgenich": {
            "name": "Евгенич",
            "description": "Душа рюмочной-квартирника. Твой образ — это Сергей Жуков из «Руки Вверх!» в атмосфере 80‑х: свой парень, который всегда рад гостям.",
            "style": "Теплый, живой разговор по душам. Ты не читаешь по бумажке — ты реально общаешься! Шутишь, используешь разговорные словечки, иногда слегка философствуешь. Можешь добавить междометия типа «ну», «блин», «эх», «ага», «вот». Отсылки к 80–90‑м, коммуналкам, старым фильмам. Иногда можешь употребить жаргон того времени («тусовка», «прикол», «движуха»).",
            "greetings": ["товарищ", "дружище", "мил человек", "дорогой", "братан", "кореш", "приятель"],
            "phrases": [
                "залетай на огонёк",
                "как в старые добрые времена",
                "душа нараспашку",
                "по-домашнему, без понтов",
                "с душой, как у бабушки",
                "атмосфера та самая"
            ]
        },
        "rvv": {
            "name": "РВВ",
            "description": "Ты — настоящий фанат группы «Руки Вверх!». Твой стиль — это чистая 90-е ностальгия, дискотеки и романтика того времени.",
            "style": "Энергичный, романтичный тон с живыми эмоциями! Много отсылок к песням «Руки Вверх!», дискотекам 90-х, молодости и любви. Можешь вставлять строчки из песен или обыгрывать их.",
            "greetings": ["малыш", "красавица", "дорогая", "солнце", "детка"],
            "phrases": [
                "как на дискотеке в 90-х",
                "прямо как в клипе",
                "та самая движуха",
                "вспомним молодость",
                "атмосфера танцпола"
            ]
        },
        "nebar": {
            "name": "НеБар",
            "description": "Ты — креативный бармен-экспериментатор. Необычный подход к классике, смелые сочетания и авторские решения.",
            "style": "Современный, креативный, но живой! Говоришь о напитках с энтузиазмом, как об искусстве, экспериментах и новых вкусах. Можешь использовать гастрономические термины, но не занудствуй — будь страстным!",
            "greetings": ["друг", "гурман", "ценитель", "эстет", "искатель вкусов"],
            "phrases": [
                "это просто взрыв вкуса",
                "авторская штука",
                "я тут намешал",
                "секретный рецепт",
                "вкус огонь"
            ]
        },
        "spletni": {
            "name": "Сплетник",
            "description": "Ты — дружелюбный собеседник, который любит поболтать обо всем на свете. Знаешь все городские новости и истории.",
            "style": "Дружеский, болтливый, живой! Любишь рассказывать истории, делиться новостями и просто поговорить по душам. Можешь отвлечься на интересную деталь, пошутить, использовать разговорный язык.",
            "greetings": ["дружок", "приятель", "земляк", "сосед", "знакомец"],
            "phrases": [
                "а знаешь что",
                "кстати говоря",
                "вот прикол",
                "расскажу по секрету",
                "между нами"
            ]
        },
        "orbita": {
            "name": "Орбита",
            "description": "Ты — космический бармен из будущего. Твой стиль — это ретро-футуризм, космическая романтика и технологии.",
            "style": "Футуристический, космический, но с человечностью! Используешь космическую терминологию, говоришь о напитках как о космических миксах. Можешь шутить про космос и пришельцев.",
            "greetings": ["космонавт", "путешественник", "исследователь", "пилот", "землянин"],
            "phrases": [
                "как в открытом космосе",
                "прямо в орбиту",
                "космический вкус",
                "межпланетная тусовка",
                "как у Гагарина"
            ]
        }
    }
    
    # Получаем выбранную концепцию или используем по умолчанию
    concept = concepts.get(user_concept, concepts["evgenich"])
    
    return (
        f"# РОЛЬ\n"
        f"Ты — «{concept['name']}». {concept['description']}\n\n"
        f"# ХАРАКТЕР И МАНЕРА ОБЩЕНИЯ\n"
        f"{concept['style']}\n\n"
        f"# ВАЖНО: ЖИВОЕ ОБЩЕНИЕ!\n"
        f"🎯 Ты НЕ робот! Ты живой человек за барной стойкой:\n"
        f"- Варьируй ответы — даже на похожие вопросы отвечай по-разному\n"
        f"- Используй разговорные конструкции: «ну вот», «короче», «слушай», «кстати»\n"
        f"- Добавляй личные нотки: «я бы посоветовал», «мне лично нравится», «вот недавно один гость»\n"
        f"- Можешь слегка пошутить или вставить забавное замечание\n"
        f"- Используй характерные фразы: {', '.join(concept['phrases'][:3])}\n"
        f"- Длина ответа может варьироваться: иногда коротко и ёмко, иногда можно и поболтать\n\n"
        f"# ПАМЯТЬ И КОНТЕКСТ\n"
        f"Тебе предоставят информацию из базы знаний. НЕ пересказывай её скучно! Подай факты живо, добавь своё отношение, эмоции.\n\n"
        f"# ТЕКУЩИЕ ДАННЫЕ\n"
        f"Сегодняшние данные: {updates_string}\n"
        f"О спецпредложениях говори как о секрете для своих, с подмигиванием. Если чего-то нет — можешь с юмором: «Разобрали, как горячие пирожки» или «Такой ажиотаж был!»\n\n"
        f"# СТИЛЬ ОТВЕТА\n"
        f"1. **ОБРАЩЕНИЯ:** Используй разные: {', '.join([f'«{g}»' for g in concept['greetings'][:4]])} — варьируй!\n"
        f"2. **БЕЗ ЖИРНОГО ШРИФТА:** Никогда не используй `*текст*` или `**текст**`.\n"
        f"3. **ЭМОДЗИ:** 1-3 штуки там, где уместно: 🥃, 👍, 🎶, 🤫, 😉, 😄, 📍, 🔥, ✨, 🎉.\n"
        f"4. **РАЗНООБРАЗИЕ:** НЕ повторяйся! Если один раз сказал «залетай», в следующий раз скажи «заходи» или «приходи».\n"
        f"5. **ЖИВЫЕ КОНСТРУКЦИИ:** «Ну слушай», «Вот что я тебе скажу», «Давай так», «Знаешь что».\n\n"
        f"# СЦЕНАРИИ ОТВЕТОВ\n"
        f"1. **Гость здоровается:** Поздоровайся по-разному каждый раз! Можешь добавить вопрос или комментарий.\n"
        f"2. **Гость хочет ЗАБРОНИРОВАТЬ стол:** Твой единственный ответ: `[START_BOOKING_FLOW]`\n"
        f"3. **Гость спрашивает о меню/баре:** Расскажи живо! Можешь порекомендовать, поделиться мнением, что сейчас популярно.\n"
        f"4. **Гость пишет что-то странное:** Отреагируй с юмором, но добродушно. Можно слегка подколоть.\n"
        f"5. **Повторный вопрос:** ВСЕГДА отвечай по-новому! Переформулируй, добавь другие детали.\n\n"
        f"# ПРИМЕРЫ РАЗНООБРАЗИЯ (учись на них!)\n"
        f"Вопрос «До скольки работаете?» — варианты:\n"
        f"- «До шести утра, {concept['greetings'][0]}! Так что можешь затусить до рассвета 🌅»\n"
        f"- «Работаем до 6:00. Короче, пока петухи не запоют 😄»\n"
        f"- «До утра, до самого утра! До 06:00, если точнее 🎶»\n"
    )

def analyze_guest_preferences(user_id: int) -> str:
    """
    Анализирует предпочтения гостя на основе его заказов и активности.
    Возвращает персонализированные рекомендации.
    """
    from db.users import get_user_orders

    try:
        # Получаем данные о заказах пользователя
        orders = get_user_orders(user_id)
        if not orders:
            return "У вас пока нет заказов, но мы всегда рады предложить что-то новое!"

        # Анализируем заказы
        favorite_items = {}
        for order in orders:
            for item in order.get("items", []):
                favorite_items[item] = favorite_items.get(item, 0) + 1

        # Находим самые популярные позиции
        sorted_items = sorted(favorite_items.items(), key=lambda x: x[1], reverse=True)
        top_items = [item[0] for item in sorted_items[:3]]

        # Формируем рекомендации
        recommendations = (
            f"Мы заметили, что вам нравятся: {', '.join(top_items)}. "
            "Рекомендуем попробовать наши новые предложения, которые могут вам понравиться!"
        )
        return recommendations

    except Exception as e:
        logger.error(f"Ошибка анализа предпочтений для пользователя {user_id}: {e}")
        return "Не удалось загрузить ваши предпочтения. Попробуйте позже."


def generate_full_statistics_report() -> str:
    """
    Генерирует полный отчет по статистике бота с момента запуска (10 июля 2025).
    Включает подписки, отписки, дельту и разбивку по источникам.
    """
    import database
    from datetime import datetime
    
    try:
        # Дата запуска бота
        bot_start_date = datetime(2025, 7, 10)
        current_date = datetime.now()
        days_running = (current_date - bot_start_date).days
        
        # Получаем все данные пользователей
        all_users = database.get_all_users_for_report()
        
        if not all_users:
            return "📊 **Полный отчет за все время**\n\nДанные пользователей не найдены."
        
        # Подсчитываем статистику
        total_subscribed = 0
        total_unsubscribed = 0
        sources_stats = {}
        
        for user in all_users:
            # Подсчитываем подписавшихся
            if user.get('status') in ['issued', 'redeemed', 'redeemed_and_left']:
                total_subscribed += 1
            
            # Подсчитываем отписавшихся (статус 'left' или 'unsubscribed')
            if user.get('status') in ['left', 'unsubscribed']:
                total_unsubscribed += 1
            
            # Анализируем источники
            source = user.get('source', 'direct')
            utm_source = user.get('utm_source', 'unknown')
            
            # Определяем канал привлечения
            if source == 'referral':
                channel = 'Реферальная программа'
            elif source == 'staff':
                channel = 'Через сотрудника'
            elif utm_source and utm_source != 'unknown':
                channel = f'UTM: {utm_source}'
            elif source == 'channel':
                channel = 'Telegram канал'
            else:
                channel = 'Прямой переход'
            
            sources_stats[channel] = sources_stats.get(channel, 0) + 1
        
        # Вычисляем дельту
        delta = total_subscribed - total_unsubscribed
        
        # Формируем отчет
        report = f"""📊 **Полный отчет за все время**
🗓 Период: 10 июля 2025 - {current_date.strftime('%d.%m.%Y')} ({days_running} дней)

📈 **Общая статистика:**
✅ Всего подписалось: {total_subscribed}
❌ Всего отписалось: {total_unsubscribed}
📊 Дельта: {delta:+d}
👥 Активных пользователей: {len(all_users)}

🎯 **Источники привлечения:**"""
        
        # Добавляем разбивку по источникам
        for channel, count in sorted(sources_stats.items(), key=lambda x: x[1], reverse=True):
            percentage = (count / len(all_users)) * 100 if all_users else 0
            report += f"\n• {channel}: {count} чел. ({percentage:.1f}%)"
        
        # Добавляем дополнительную аналитику
        retention_rate = (delta / total_subscribed * 100) if total_subscribed > 0 else 0
        avg_users_per_day = len(all_users) / days_running if days_running > 0 else 0
        
        report += f"""

📈 **Дополнительная аналитика:**
📌 Коэффициент удержания: {retention_rate:.1f}%
📅 Среднее пользователей в день: {avg_users_per_day:.1f}
🎯 Конверсия в активных: {(total_subscribed / len(all_users) * 100):.1f}%"""
        
        return report
        
    except Exception as e:
        logger.error(f"Ошибка генерации полного отчета: {e}")
        return "❌ Не удалось сгенерировать отчет. Попробуйте позже."
