# 🎨 Админ-панель бота Евгенич

Веб-интерфейс для управления ботом

## 🔐 Доступ

- **URL**: `https://your-app.up.railway.app`
- **Логин**: `admin`
- **Пароль**: `Evgenich83`

## ⚡ Возможности

### 📝 Управление текстами
- Редактирование приветствий
- Тексты меню и бронирования
- Сообщения об ошибках
- Подтверждения

### 🍷 Управление барами
- Добавить новый бар
- Редактировать название и код AMO
- Удалить бар
- Изменить эмодзи и callback_id

### 🤖 Настройки AI
- Системный промпт
- Тон общения (дружелюбный, профессиональный)
- База знаний о баре
- Параметры генерации (температура, модель)
- Тестирование ответов AI

### 👥 Управление персоналом
- **Боссы** - полный доступ
- **Админы** - админка + отправка броней
- **СММ** - только отправка броней

## 🚀 Деплой на Railway

### Создание нового сервиса

1. Откройте Railway Dashboard
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите репозиторий `evgenich-gift`
4. Нажмите "Add Service" → "Web Service"

### Настройки сервиса

**Settings → Build:**
- **Root Directory**: `web`
- **Build Command**: оставить пустым (используется `railway.toml`)
- **Start Command**: оставить пустым (используется `railway.toml`)

Railway автоматически прочитает конфигурацию из `web/railway.toml`:
```toml
[build]
buildCommand = "pip install -r web_requirements.txt"

[deploy]
startCommand = "gunicorn web_admin:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
```

**Settings → Variables:**
```bash
PORT=5000
FLASK_SECRET_KEY=evgenich-secret-2026-very-secure
```

**Settings → Networking:**
- Нажмите "Generate Domain"
- Получите публичный URL

### Готово! 🎉

Админ-панель будет доступна по адресу: `https://your-service.up.railway.app`

### ❌ Решение проблем

**"Application failed to respond":**
- Проверьте, что Root Directory = `web`
- Проверьте переменную `PORT` в Settings → Variables
- Посмотрите Deploy Logs и Application Logs

**"ModuleNotFoundError":**
- Убедитесь, что `web_requirements.txt` содержит все зависимости
- Проверьте Build Logs

**Конфиги не сохраняются:**
- Теперь конфиги сохраняются в `web/admin_config/` (относительный путь)
- При каждом деплое создаются заново (если хотите сохранить - используйте Railway Volume)

## 📂 Структура конфигов

Все настройки сохраняются в `web/admin_config/` (рядом с приложением):
- `texts.json` - тексты бота
- `bars.json` - список баров
- `ai_settings.json` - настройки AI
- `staff.json` - персонал

**Важно:** Конфиги пересоздаются при каждом деплое. Для постоянного хранения настройте Railway Volume.

## 🔄 Применение изменений

После изменения настроек в админке:
1. Бары - требуется перезапуск бота
2. Тексты - требуется перезапуск бота  
3. AI - применяется сразу
4. Персонал - требуется перезапуск бота

## 🛡️ Безопасность

- Авторизация по логину/паролю
- Сессии с автоматическим истечением (12 часов)
- Хеширование паролей
- Логирование всех действий

## 📝 Примечания

- После первого запуска создаются дефолтные конфиги
- Конфиги сохраняются в JSON и могут быть отредактированы вручную
- Для интеграции с ботом нужно подключить чтение конфигов из `/data/admin_config/`

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи Railway
2. Убедитесь что все переменные окружения установлены
3. Проверьте что директория `/data` имеет права на запись
