# 🎛️ Полная интеграция бота с админ-панелью

## ✅ Что уже работает

### 1. Админ-панель (https://evgenich-gift-production-bdf4.up.railway.app)

**Управление через веб-интерфейс:**
- 📝 **Тексты** - все сообщения бота
- 🍷 **Бары** - добавление/редактирование баров с тегами, телефонами, меню
- 🔗 **Ссылки** - URL меню, форма брони, телефоны, соцсети
- 🤖 **AI** - настройки ассистента (промпты, параметры)
- 👥 **Персонал** - управление ролями (боссы, админы, СММ)
- 📊 **Дашборд** - статистика (подключается к БД бота)

### 2. Интеграция с ботом

**Бот читает настройки из:**
- `web/admin_config/texts.json` - тексты бота
- `web/admin_config/bars.json` - список баров
- `web/admin_config/links.json` - ссылки
- `web/admin_config/ai_settings.json` - настройки AI
- `web/admin_config/staff.json` - роли персонала

**Что уже интегрировано:**
- ✅ Бары загружаются динамически при бронировании
- ✅ Тексты можно менять через админку
- ✅ Роли персонала читаются из админки + переменные окружения
- ✅ Фоллбэк на значения по умолчанию если конфиги недоступны

## 🚀 Настройка Railway

### Для админ-панели (сервис `admin-panel`)

В **Settings → Variables** добавь:

```bash
# Обязательные
FLASK_SECRET_KEY=evgenich-secret-2026-very-secure
PORT=8080

# Для подключения к БД бота (для статистики)
DATABASE_URL=${Postgres.DATABASE_URL}
USE_POSTGRES=true
GOOGLE_CREDENTIALS_JSON=(скопируй из основного сервиса)
GOOGLE_SHEET_KEY=(скопируй из основного сервиса)
```

### Для бота (сервис `evgenich-gift-production`)

Убедись, что есть все переменные (уже должны быть):
```bash
BOT_TOKEN=...
DATABASE_URL=...
USE_POSTGRES=true
BOSS_ID=196614680, 208281210
SMM_IDS=1334453330, 208281210
# и т.д.
```

## 📱 Как использовать

### 1. Изменение текстов

1. Зайди в админку → **Тексты**
2. Отредактируй нужные сообщения
3. Нажми **"Сохранить"**
4. **Перезапусти бота** на Railway

### 2. Добавление нового бара

1. Админка → **Бары** → **"Добавить новый бар"**
2. Заполни:
   - Название: `Васильевский`
   - Код AMO: `ЕВГ_СПБ_ВАС`
   - Эмодзи: `🌊`
   - Callback ID: `bar_vasilevskiy`
   - Тег: `спб_васильевский`
   - Телефон: `+7 (812) 111-22-33`
   - Ссылка на меню: `https://spb.evgenich.bar/vasilevskiy`
3. Нажми **"Добавить"**
4. **Перезапусти бота**

Новый бар появится в списке при бронировании!

### 3. Управление персоналом

1. Админка → **Персонал**
2. Добавь пользователя в нужную роль (по Telegram ID):
   - **Босс** - полный доступ ко всему
   - **Админ** - админка + отправка броней
   - **СММ** - только отправка броней
3. Нажми **"Добавить"**
4. **Перезапусти бота**

### 4. Изменение ссылок

1. Админка → **Ссылки**
2. Измени нужные URL (меню, WhatsApp, Instagram и т.д.)
3. Нажми **"Сохранить"**
4. **Перезапусти бота**

### 5. Настройка AI

1. Админка → **AI**
2. Измени:
   - Системный промпт
   - Тон общения
   - Базу знаний
   - Параметры (температура, модель)
3. Протестируй прямо в админке (кнопка "Тестировать")
4. Нажми **"Сохранить"**
5. **Перезапусти бота**

## 🔄 Перезапуск бота

После любых изменений в админке нужно перезапустить бота:

**Через Railway:**
1. Открой сервис `evgenich-gift-production`
2. Нажми **"Redeploy"** или **"Restart"**

**Альтернатива - автоперезагрузка:**
Можно добавить webhook в админку для автоматического перезапуска бота при изменениях (требует доработки).

## 📊 Просмотр статистики

1. Зайди в админку → **Дашборд**
2. Увидишь:
   - Общее количество пользователей
   - Выданные купоны за 24 часа
   - Топ рефералов
   - Последние активности
   - Топ персонала

**Важно:** Для работы статистики админ-панель должна иметь доступ к БД бота (переменная `DATABASE_URL`).

## 🔐 Безопасность

- Админ-панель защищена логином/паролем (`admin` / `Evgenich83`)
- Сессии истекают через 12 часов
- Конфиги хранятся в `web/admin_config/` (не в git)
- При деплое конфиги создаются заново (используй Volume для сохранения)

## ⚠️ Известные ограничения

1. **Изменения требуют перезапуска** - бот не подхватывает изменения на лету
2. **Конфиги не сохраняются между деплоями** - при новом деплое админ-панели конфиги сбрасываются (настрой Railway Volume для постоянного хранения)
3. **Статистика доступна только при подключении к БД** - нужны переменные окружения

## 🛠️ Следующие улучшения

### Запланировано:
- [ ] Горячая перезагрузка конфигов без перезапуска бота
- [ ] Railway Volume для постоянного хранения конфигов
- [ ] Webhook для автоматического обновления бота при изменениях
- [ ] Экспорт/импорт конфигов
- [ ] История изменений конфигов
- [ ] Ролевая модель доступа к админке (разные уровни доступа)

## 🆘 Помощь

**Проблемы?**
1. Проверь логи в Railway (Deploy Logs и Application Logs)
2. Убедись, что все переменные окружения установлены
3. Проверь, что бот перезапущен после изменений в админке
4. Проверь, что конфиги создались в `web/admin_config/`

**Тестирование локально:**
```bash
cd /workspaces/evgenich-gift/web
python web_admin.py
# Открой http://localhost:5000
```
